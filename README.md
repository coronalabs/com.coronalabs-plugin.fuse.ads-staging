Sources for the plugin `plugin.fuse.ads-staging`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.fuse.ads-staging" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
